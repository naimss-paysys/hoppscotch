networks:
  hopps-net:
    driver: bridge
    name: hopps-net

volumes:
  hopps_postgres_data:
    name: hopps_postgres_data
  hopps_redis_data:
    name: hopps_redis_data

# ──────────────────────────────────────────────────────────────
# TWO URL VARIABLES in .env — only these change per environment:
#
#   PUBLIC_URL   = full HTTP/HTTPS URL  (e.g. https://xxx.trycloudflare.com)
#   PUBLIC_WS_URL= full WS/WSS URL      (e.g. wss://xxx.trycloudflare.com)
#
# Local (no tunnel):  PUBLIC_URL=http://api.paysyslabs.com:8090
#                     PUBLIC_WS_URL=ws://api.paysyslabs.com:8090
# Cloudflare tunnel:  run ./tunnel-restart.sh — it sets both automatically
# ──────────────────────────────────────────────────────────────

services:

  # ────────────────────────────────────────────
  # PostgreSQL for Hoppscotch
  # ────────────────────────────────────────────
  postgres-hopps:
    image: postgres:15-alpine
    container_name: hopps_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: hoppscotch
      POSTGRES_PASSWORD: REDACTED_DB_PASS
      POSTGRES_DB: hoppscotch
    volumes:
      - hopps_postgres_data:/var/lib/postgresql/data
    networks:
      - hopps-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hoppscotch -d hoppscotch"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m

  # ────────────────────────────────────────────
  # Redis
  # ────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: hopps_redis
    restart: unless-stopped
    command: >
      redis-server
      --requirepass REDACTED_REDIS_PASS
      --appendonly yes
      --maxmemory 200mb
      --maxmemory-policy allkeys-lru
    volumes:
      - hopps_redis_data:/data
    networks:
      - hopps-net
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "REDACTED_REDIS_PASS", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 256m

  # ────────────────────────────────────────────
  # SMTP Bridge — accepts SMTP from backend,
  # calls PaysysLabs HTTP email API
  # ────────────────────────────────────────────
  smtp-bridge:
    build: ../smtp-bridge
    image: hopps_smtp_bridge
    container_name: hopps_smtp_bridge
    restart: unless-stopped
    environment:
      EMAIL_API_URL: http://10.0.150.16:7034/api/EmailService
      FROM_NAME: paysyslabs
      FROM_EMAIL: watcher@paysyslabs.com
      PUBLIC_URL: ${PUBLIC_URL}
      ADMIN_URL: ${PUBLIC_URL}/admin
      BACKEND_URL: http://hoppscotch-backend:8080
      GITHUB_CLIENT_ID: REDACTED_GITHUB_CLIENT_ID
      GITHUB_CLIENT_SECRET: REDACTED_GITHUB_SECRET
      GITHUB_CALLBACK_URL: ${PUBLIC_URL}/api/auth/github/callback
    networks:
      - hopps-net
    deploy:
      resources:
        limits:
          memory: 64m

  # ────────────────────────────────────────────
  # Hoppscotch Proxy — server-side CORS proxy
  # /proxy/ → CORS bypass for Hoppscotch proxy mode
  # /forward → transform + sign + forward to Spring
  # ────────────────────────────────────────────
  hopps-proxy:
    build: ../hopps-proxy
    image: hopps_proxy
    container_name: hopps_proxy
    restart: unless-stopped
    extra_hosts:
      - "api.paysyslabs.com:REDACTED_SERVER_IP"
      - "mixxmmp-test.tigo.co.tz:10.245.0.169"
    networks:
      - hopps-net
    deploy:
      resources:
        limits:
          memory: 64m

  # ────────────────────────────────────────────
  # Hoppscotch Backend
  # Internal port: 8080 (NOT 3170)
  # ────────────────────────────────────────────
  hoppscotch-backend:
    image: hoppscotch/hoppscotch-backend:latest
    container_name: hopps_backend
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql://hoppscotch:REDACTED_DB_PASS@postgres-hopps:5432/hoppscotch
      REDIS_URL: redis://:REDACTED_REDIS_PASS@redis:6379
      VITE_BASE_URL: ${PUBLIC_URL}
      VITE_SHORTCODE_BASE_URL: ${PUBLIC_URL}
      VITE_ADMIN_URL: ${PUBLIC_URL}/admin
      VITE_BACKEND_GQL_URL: ${PUBLIC_URL}/api/graphql
      VITE_BACKEND_WS_URL: ${PUBLIC_WS_URL}/api/graphql
      VITE_BACKEND_API_URL: ${PUBLIC_URL}/api
      VITE_ALLOWED_AUTH_PROVIDERS: email,github
      GITHUB_CALLBACK_URL: ${PUBLIC_URL}/api/auth/github/callback
      VITE_PROXY_URL: ${PUBLIC_URL}/proxy/
      WHITELISTED_ORIGINS: ${PUBLIC_URL},http://api.paysyslabs.com:8090,http://api.paysyslabs.com:8091,http://REDACTED_SERVER_IP:8090,http://localhost:8090
    depends_on:
      postgres-hopps:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - hopps-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/ping || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 12
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 1g

  # ────────────────────────────────────────────
  # Hoppscotch Frontend
  # ────────────────────────────────────────────
  hoppscotch-frontend:
    image: hoppscotch/hoppscotch-frontend:latest
    container_name: hopps_frontend
    restart: unless-stopped
    environment:
      VITE_BASE_URL: ${PUBLIC_URL}
      VITE_SHORTCODE_BASE_URL: ${PUBLIC_URL}
      VITE_ADMIN_URL: ${PUBLIC_URL}/admin
      VITE_BACKEND_GQL_URL: ${PUBLIC_URL}/api/graphql
      VITE_BACKEND_WS_URL: ${PUBLIC_WS_URL}/api/graphql
      VITE_BACKEND_API_URL: ${PUBLIC_URL}/api
      VITE_ALLOWED_AUTH_PROVIDERS: email,github
      VITE_PROXY_URL: ${PUBLIC_URL}/proxy/
    depends_on:
      hoppscotch-backend:
        condition: service_healthy
    networks:
      - hopps-net
    deploy:
      resources:
        limits:
          memory: 256m

  # ────────────────────────────────────────────
  # Hoppscotch Admin
  # ────────────────────────────────────────────
  hoppscotch-admin:
    image: hoppscotch/hoppscotch-admin:latest
    container_name: hopps_admin
    restart: unless-stopped
    environment:
      ENABLE_SUBPATH_BASED_ACCESS: "true"
      VITE_BASE_URL: ${PUBLIC_URL}
      VITE_SHORTCODE_BASE_URL: ${PUBLIC_URL}
      VITE_ADMIN_URL: ${PUBLIC_URL}/admin
      VITE_BACKEND_GQL_URL: ${PUBLIC_URL}/api/graphql
      VITE_BACKEND_WS_URL: ${PUBLIC_WS_URL}/api/graphql
      VITE_BACKEND_API_URL: ${PUBLIC_URL}/api
      VITE_ALLOWED_AUTH_PROVIDERS: email,github
      VITE_PROXY_URL: ${PUBLIC_URL}/proxy/
    depends_on:
      hoppscotch-backend:
        condition: service_healthy
    networks:
      - hopps-net
    deploy:
      resources:
        limits:
          memory: 256m

  # ────────────────────────────────────────────
  # Nginx Reverse Proxy
  # Routes all traffic on port 8090
  # ────────────────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: hopps_nginx
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro,z
      - ./nginx/magic.html:/usr/share/nginx/html/magic.html:ro,z
    depends_on:
      hoppscotch-frontend:
        condition: service_started
      hoppscotch-admin:
        condition: service_started
      hoppscotch-backend:
        condition: service_healthy
    networks:
      - hopps-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 128m

  # ────────────────────────────────────────────
  # ngrok — stable static domain tunnel
  # Domain: dander-pork-headgear.ngrok-free.dev (never changes)
  # GitHub OAuth callback set once, never updated again
  # ────────────────────────────────────────────
  ngrok:
    image: ngrok/ngrok:latest
    container_name: hopps_ngrok
    restart: unless-stopped
    command: http --url=dander-pork-headgear.ngrok-free.dev nginx:80
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    depends_on:
      nginx:
        condition: service_healthy
    networks:
      - hopps-net
    deploy:
      resources:
        limits:
          memory: 64m
